[Unit]
Description=Prometheus exporter for Netweaver clusters metrics
After=network.target

[Service]
Type=simple
Restart=always
ExecStart=/usr/bin/sap_host_exporter
ExecReload=/bin/kill -HUP $MAINPID

[Install]
WantedBy=multi-user.target
